{% extends 'base.html.twig' %}

{% block title %}Presentation{% endblock %}

{% block body %}
    <h1>Presentation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ presentation.id }}</td>
            </tr>
            <tr>
                <th>Photo_presentation</th>
                <td>{{ presentation.photoPresentation }}</td>
            </tr>
            <tr>
                <th>Contenu_presentation</th>
                <td>{{ presentation.contenuPresentation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('presentation_index') }}">back to list</a>

    <a href="{{ path('presentation_edit', {'id': presentation.id}) }}">edit</a>

    {{ include('presentation/_delete_form.html.twig') }}
{% endblock %}
